{{- if .Values.services.idFront.enabled }}
apiVersion: v1
kind: Service
metadata:
  namespace: {{ include "liquio.namespace" . }}
  name: {{ include "liquio.fullname" . }}-id-front
  labels:
    {{- include "liquio.labels" . | nindent 4 }}
    app.kubernetes.io/component: id-front
spec:
  type: {{ .Values.services.idFront.type }}
  ports:
    - port: {{ .Values.services.idFront.servicePort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liquio.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: id-front
{{- end }}
