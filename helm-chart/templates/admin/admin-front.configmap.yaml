apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ include "liquio.namespace" . }}
  name: {{ include "liquio.fullname" . }}-admin-front-config
  labels:
    {{- include "liquio.labels" . | nindent 4 }}
{{ $configBase := dict
  "application" (dict
    "name" "Admin Panel"
    "environment" "development"
    "type" "adminpanel"
  )
  "customers" (list
    (dict "id" 1 "name" "bpmn")
  )
  "registry" (dict
    "useReindex" true
    "useAfterhandlersReindex" true
  )
  "reports" (dict
    "enabled" true
  )
  "useUIFilters" true
  "customInterfaces" true
  "plugins" (list)
  "backendUrl" (printf "%s://admin-api.%s" (include "liquio.scheme" .) .Values.global.domain)
  "authLink" (printf "%s://admin-api.%s/redirect/auth" (include "liquio.scheme" .) .Values.global.domain)
  "adminPanelUrl" (printf "%s://admin.%s" (include "liquio.scheme" .) .Values.global.domain)
  "cabinetUrl" (printf "%s://cabinet.%s" (include "liquio.scheme" .) .Values.global.domain)
  "sentryDns1" "https://sentry"
  "sessionLifeTime" 480
  "enabledMock" false
  "clientId" "liquio-portal"
  "idAuthLink" (printf "%s://id.%s/authorise" (include "liquio.scheme" .) .Values.global.domain)
  "enabledDeleteUser" false
  "enabledMocksPage" false
  "defaultLanguage" "en"
}}
data:
  config.json: |
{{ include "liquio.renderJsonConfig" (dict "ctx" . "service" "adminFront" "config" "config" "base" $configBase) | nindent 4 }}
