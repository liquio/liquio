{{- if .Values.services.adminFront.enabled }}
apiVersion: v1
kind: Service
metadata:
  namespace: {{ include "liquio.namespace" . }}
  name: {{ include "liquio.fullname" . }}-admin-front
  labels:
    {{- include "liquio.labels" . | nindent 4 }}
    app.kubernetes.io/component: admin-front
spec:
  type: {{ .Values.services.adminFront.type }}
  ports:
    - port: {{ .Values.services.adminFront.servicePort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liquio.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: admin-front
{{- end }}
