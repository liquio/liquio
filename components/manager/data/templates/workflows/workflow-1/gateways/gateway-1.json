{
  "formulas": [
    {
      "condition": "(documents) => { return !documents.find(item => item.documentTemplateId === 2).data.order.approved; }",
      "isDefault": true
    },
    {
      "condition": "(documents) => { return documents.find(item => item.documentTemplateId === 2).data.order.approved; }",
      "isDefault": false
    }
  ]
}
