{
  "formulas": [
    {
      "condition": "(documents) => { return documents.find(item => item.documentTemplateId === 10).data.form.approved; }",
      "isDefault": true
    },
    {
      "condition": "(documents) => { return !documents.find(item => item.documentTemplateId === 10).data.form.approved; }",
      "isDefault": false
    }
  ]
}
