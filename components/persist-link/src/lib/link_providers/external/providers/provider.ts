/**
 * Provider.
 */
class Provider {
  /**
   * Provider constructor.
   * @param {object} config Config.
   */
  constructor(config) {
    // Save config.
    this.config = config;
  }
}

// Export.
export default Provider;
