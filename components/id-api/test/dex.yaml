issuer: http://localhost:5556
storage:
  type: memory

web:
  http: 0.0.0.0:5556

telemetry:
  http: 0.0.0.0:5557

oauth2:
  skipApprovalScreen: true

connectors:
  - type: mockCallback
    id: mock
    name: Mock
    config:
      username: testuser@example.com

staticClients:
  - id: test-oidc-client-id
    redirectURIs:
      - 'http://localhost:8080/authorise/oidc/dex/callback'
    name: 'Test OIDC Client'
    secret: test-oidc-secret
