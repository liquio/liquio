/**
 * Model.
 */
class Model {
  /**
   * Model constructor.
   * @param {object} config Config.
   */
  constructor(config) {
    this.db = db;
    this.config = config;
  }
}

// Export.
module.exports = Model;
