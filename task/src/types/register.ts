interface RegisterRecord {
    id: string,
    registerId: number,
    keyId: number,
    data: object,
    meta: object,
    allowTokens: string[],
    createdBy: string,
    updatedBy: string,
    createdAt: string,
    updatedAt: string
}